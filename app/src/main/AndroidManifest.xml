<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.aufgabe1">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:debuggable="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".Activities.FirstLoginView"></activity>
        <activity android:name=".Activities.ThxForRegView"></activity>
        <activity android:name=".Activities.DataPrivacy"></activity>
        <activity android:name=".Activities.PersonalData"></activity>
        <activity android:name=".Activities.AddressData"></activity>
        <activity android:name=".Activities.ReadyToStart"></activity>
        <activity android:name=".Activities.Dashboard"></activity>
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegView"></activity>


    </application>

</manifest>